apiVersion: gateway.kyma-project.io/v2
kind: APIRule
metadata:
  name: {{ .Values.services.app.name }}
spec:
  gateway: {{ .Values.gateway }}
  hosts: 
  - {{ .Values.services.app.name }}-{{ .Release.Namespace }}.{{ .Values.clusterDomain }}
  rules:
    - path: /*
      noAuth: true
      methods:
        - HEAD
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
  service:
    name: {{ .Values.services.app.name }}
    port: {{ .Values.services.app.service.port }}