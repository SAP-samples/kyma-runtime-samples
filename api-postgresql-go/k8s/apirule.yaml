apiVersion: gateway.kyma-project.io/v2
kind: APIRule
metadata:
  name: api-postgresql-go
spec:
  hosts:
    - api-postgresql-go
  service:
    name: api-postgresql-go
    port: 80
  gateway: kyma-system/kyma-gateway
  corsPolicy: 
    allowHeaders:
      - Content-Type
    allowMethods:
      - GET
      - POST
      - PUT
      - DELETE
      - OPTIONS
    allowOrigins:
      - regex: ".*{YOUR_KYMA_DOMAIN}"
  rules:
    - path: "/orders"
      methods:
      - GET
      - POST
      - PUT
      - DELETE
      - OPTIONS
      noAuth: true
    - path: "/orders/{**}"
      methods:
      - GET
      - POST
      - PUT
      - DELETE
      - OPTIONS
      noAuth: true