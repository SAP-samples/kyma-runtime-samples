apiVersion: gateway.kyma-project.io/v2
kind: APIRule
metadata:
  name: fe-ui5-mssql
spec:
  gateway: kyma-system/kyma-gateway
  hosts:
  - fe-ui5-mssql
  rules:
    - path: /*
      noAuth: true
      methods:
        - GET
  service:
    name: fe-ui5-mssql
    port: 80
